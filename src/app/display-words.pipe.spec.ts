import { DisplayWordsPipe } from './display-words.pipe';

describe('DisplayWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
